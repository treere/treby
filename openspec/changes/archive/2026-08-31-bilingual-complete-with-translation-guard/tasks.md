## 1. Translation guard infrastructure

- [x] 1.1 Create Mix task `mix treby.check_translations` at `lib/mix/tasks/check_translations.ex` that parses `priv/gettext/default.pot` and `priv/gettext/it/LC_MESSAGES/default.po` (and `errors.po`), counts total/missing, prints per-key missing list with POT source refs, exits 1 on missing `it` translations or stale POT (`--check-pot` mode), and exits 0 otherwise — verify with `mix treby.check_translations` and `mix treby.check_translations --help`.
- [x] 1.2 Create custom Credo check `Treby.Credo.Check.NoHardcodedUIStrings` at `lib/treby/credo/check/no_hardcoded_ui_strings.ex` (via `mix credo.gen.check`), implementing `Credo.check` to flag hardcoded UI literals in `lib/treby_web/live/**`, `components/**`, `controllers/**` outside `gettext`/`ngettext` in `~H` templates and `put_flash`/assigns, with allowlist for `class`/`id`/`phx-*`/`hero-*` and `credo:disable` escape hatch — register in `.credo.exs` (`requires` + `checks.enabled`), verify `mix credo --strict` flags a dummy `"My Actions"` literal and passes when wrapped as `gettext("My Actions")`.
- [x] 1.3 Wire both guards into `mix precommit` alias in `mix.exs` (add `credo --strict` + `treby.check_translations`) and add explicit `Check translations` + `Credo` steps to `.github/workflows/ci.yml` (run `mix gettext.extract` dry-check + `mix treby.check_translations` + `mix credo --strict`), verify CI workflow contains both steps and `mix precommit` fails when either a dummy empty `msgstr` is inserted or a hardcoded string is introduced.

## 2. Full-application gettext wrapping — LiveView/UI

- [x] 2.1 Wrap all hardcoded user-facing strings in `lib/treby_web/live/dashboard_live.ex` (title, `Welcome, %{name}!`, weekly-stats labels, My Actions headings, "Scorecards to fill"/"Waiting on others"/"Fill scorecard", empty states "All caught up"/"No upcoming interviews"/"No stale candidates"/"No open jobs yet"/"Create your first job", onboarding checklist labels, activity labels, interviewer placeholder, flash messages) with `gettext`/`gettext/2` interpolation — verify with `rg 'gettext' lib/treby_web/live/dashboard_live.ex` and `mix compile --warnings-as-errors`.
- [x] 2.2 Sweep remaining authenticated LiveViews and components for hardcoded UI strings: `lib/treby_web/live/candidates_live/**`, `lib/treby_web/live/jobs_live/**`, `lib/treby_web/live/interviews_live/**`, `lib/treby_web/live/pipeline_live/**`, `lib/treby_web/live/settings_live/**`, `lib/treby_web/components/layouts.ex` and `core_components.ex` — replace raw literals with `gettext`, keep `msgid` static and use keyword interpolation — verify via `rg '"[A-Z][^"]{2,}"' lib/treby_web/live lib/treby_web/components | grep -v gettext | grep -v 'defmodule\|@moduledoc\|#\|class='` audit shows no user-facing literals outside `gettext`.
- [x] 2.3 Wrap remaining flash/error messages in `lib/treby_web/controllers/**` and any email subject/body strings exposed in UI with `gettext` (preserve interpolation vars like `%{email}`, `%{tenant}`) — verify `mix gettext.extract --merge` picks up new keys and `mix compile` passes.

## 3. Catalog extraction and Italian translations

- [x] 3.1 Run `mix gettext.extract --merge` to regenerate `priv/gettext/default.pot` / `errors.pot` and merge into `priv/gettext/{en,it}/LC_MESSAGES/*.po`; review diff for expected new `msgid`s from dashboard + sweep — verify `priv/gettext/default.pot` updated and `mix treby.check_translations` now reports missing `it` keys.
- [x] 3.2 Fill all missing `msgstr` in `priv/gettext/it/LC_MESSAGES/default.po` and `errors.po` for newly extracted keys (Italian translations; keep `en` empty as fallback) — verify `mix treby.check_translations` passes (0 missing, 100% coverage) and `priv/gettext/it/LC_MESSAGES/default.po` has no `msgstr ""` for non-header entries.
- [x] 3.3 Manual QA: set locale to `it` in Settings → Language and visit dashboard, jobs, candidates, pipeline, interviews, settings, candidate portal, and career page; confirm no English leaks in headings/labels/empty states/flash — fix any missed wraps and re-extract.

## 4. Tests

- [x] 4.1 Add/extend tests for dashboard localization: LiveView test visits dashboard with `locale: "it"` (via `SetLocale` session) and asserts translated headings/labels/empty states appear; with `locale: "en"` asserts English — run `mix test test/treby_web/live/dashboard_live_test.exs` (or new `test/treby_web/live/dashboard_i18n_test.exs`).
- [x] 4.2 Add test for translation guard: unit test for `Mix.Tasks.Treby.CheckTranslations` parsing logic (missing detection, stale POT detection, `en` skipped) plus an integration assertion that `mix treby.check_translations` passes on the committed catalog — run `mix test test/mix/tasks/check_translations_test.exs`.
- [x] 4.3 Add test for Credo check: unit test for `Treby.Credo.Check.NoHardcodedUIStrings` asserting it flags raw `"My Actions"` in a `~H` template and does not flag `gettext("My Actions")`, and that it ignores `class`/`hero-*` literals — run `mix test test/treby/credo/check/no_hardcoded_ui_strings_test.exs`.
- [x] 4.4 Add i18n regression test: assert that `priv/gettext/it/LC_MESSAGES/default.po` has zero empty `msgstr` after extraction (can reuse guard logic) to prevent future drift — run `mix test`.

## 5. Specs and docs

- [x] 5.1 Update main specs: copy delta specs to `openspec/specs/translation-coverage-guard/spec.md` (new), and merge `i18n` + `dashboard` deltas into `openspec/specs/i18n/spec.md` and `openspec/specs/dashboard/spec.md` (preserve Purpose/Requirements, add bilingual requirements) — verify `openspec validate --strict` passes for those specs.
- [x] 5.2 Sync user manual: update `site/features/dashboard.md` (note bilingual dashboard) and create/refresh `site/features/i18n.md` or language section in `site/getting-started.md` describing how users switch language (Settings → Language) and how contributors add strings (`gettext` + `mix gettext.extract --merge` + guard) — update `site/.vitepress/config.ts` sidebar and `site/features/index.md` if new page, run `cd site && npm run build`, regenerate screenshots with `node scripts/screenshots.mjs`.
- [x] 5.3 Run `mix precommit` and `openspec validate --strict` and fix all issues — verify both commands pass with zero warnings/errors.
