## 1. UI

- [x] 1.1 `lib/treby_web/live/schedule_live/index.ex` — add ad-hoc fallback form when no eligible examiners
- [x] 1.2 `lib/treby_web/live/pipeline_live/index.ex` — add `Schedule interview` CTA on Interview cards in zero-state

## 2. Tests

- [x] 2.1 Add test for zero-state scheduling
- [x] 2.2 Run `mix test` and `mix precommit`

## 3. Final checks

- [x] 3.1 Playwright smoke: fresh tenant schedule without availability
